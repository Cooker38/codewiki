# 2026-07-14 工作日志

## explore 走什么查询（答疑）
- `codewiki_explore` = 符号定位(node 表 name/qualified_name/id 查，**不走 FTS**) + 多段定点查询 + Python BFS 图遍历。
- 各阶段 SQL：callers/callees 走 `edges WHERE source/target=? AND kind IN ('calls','references','imports','instantiates','uses')`；impact 走 `edges WHERE target=?` 全种类并过滤 `contains`；ancestor 走 `edges WHERE target=? AND kind='contains'`；源码/整簇源码是**读文件**(非 SQL)；cluster dump 封顶 4 节点。

## 用 explore-only 重测 codewiki（用户："不做class，做explore"）
- 含义澄清：用户要的是"只用 explore 工具搜索重测 codewiki"，不是改代码（已用 AskUserQuestion 确认）。
- 写了 harness `benchmark_explore/run_codewiki_explore.py`：对 6 个符号（runScript/RunScriptTool/ScriptExecutionService/McpServerConfig/ChatClientConfig/ScriptController）调 `codewiki.mcp_server.codewiki_explore`，输出落 `benchmark_explore/codewiki_explore_outputs.txt`。
- 测试库 `/tmp/dc_bench/.codewiki/codewiki.db` 现状：7437 节点 / 14439 边 / 52 route 节点 / 251 uses 边 / 7118 contains 边——修复均已落地。
- codegraph 侧 `explore` 在沙箱被拦：它向 `C:\Users\25692\Desktop\dataAgent\.codegraph` 写索引，沙箱禁止工作区外写，故未能并排生成（须 dangerouslyDisableSandbox 才能跑，未获授权未执行）。

## 维护：清理超长 MEMORY.md
- 原 MEMORY.md 被截断注入，已重写精简版（合并重复、保留决策相关事实）。

## 检查 P1/P2/P3 收敛 + 同步历史决策到文档
- 收敛结论：**P1 收敛**（15 验收 14✅ + 2 非阻塞⚠️：field access 未专门提取、idempotent 靠删库重建）；**P2 收敛**（10/10，MCP=3 工具对齐 codegraph explore-only）；**P3 收敛**（设计+实现到位，6✅ + 4⚠️ 仅余真实 git 提交场景端到端手测未跑，非设计缺口）。
- 发现并修正文档与锁定决策不一致：
  - `项目整体方向.md` §7 / `技术架构与开发安排.md` §四 的 **P2 行仍写"8 个 MCP 工具"** → 改为"MCP 仅 3 工具 + CLI 8 命令"。
  - `项目整体方向.md` §2.1 / §5.4 与 `技术架构与开发安排.md` §2.3 把 **窄工具(callers/callees/impact/node/search)列为 MCP 工具** → 澄清 MCP 仅暴露 `codewiki_explore`（内含这些事实），窄工具仅 CLI。
  - 新增决策入档：explore「整簇源码(cluster)」+ 类级聚合 `_aggregate_seeds`；Spring `uses` 边（@Bean 参数注入/boot-context/@Bean 返回类型链）并入 `_CALL_EDGE_KINDS`；**MCP stdio 接入约定**（command 须为 .exe，禁用 .bat；零绝对路径配置）。
  - `P3-Git增量同步.md` 约束 5 的"P2 的 8 个工具不变" → 改为"P2 的 3 个 MCP 工具签名不变"。
  - 更新 `specs/收敛状态评估.md` 日期为 2026-07-14，补 07-14 explore 增强与 Spring uses 边落地说明。

## 采用率诊断：MCP 调用率过低，agent 偏向 grep/read（新问题）
- 现象：CodeWiki 引擎已收敛，但 agent 实际调用 `codewiki_*` 频率低，更爱用 grep/Read。
- 根因（已读 mcp_server.py 实证）：① **最关键** MCP 只暴露 explore/init/sync 3 工具；node/callers/callees/impact/search 仅是函数未加 `@mcp.tool()`，对 agent 不可见；`codewiki_search`(FTS5) 只在 CLI，MCP 无"按关键词发现符号"入口 → agent 不知符号名时只能 grep 起头。② explore 太重（~25K 字符 dump），廉价查找被 grep 抢走。③ AGENTS.md 桥接弱（只说"先试 explore"，没教"不知符号名怎么办"，且与①自相矛盾）。④ explore 预算提示 L462 写"fall back to Read"反向背书 grep/Read。⑤ 冷启动/时效性摩擦（DB 缺失/落后 HEAD）训练 agent 远离 codewiki。
- 优化方案（未改码，供决策）：A' explore miss 自动回退 FTS 搜索（推荐，不动 3-工具锁定）；A 暴露 `codewiki_search` 为 MCP（打破锁定）；B explore 加 detail 档；C 重写 AGENTS.md 决策树；D 冷启动鲁棒化；E 去反向背书文案；F 埋点度量调用率。
- 推荐组合：短期 A'+B+C+E；中期 D，必要时 A。已写入 `resource/问题诊断与根因分析.md` 第九章。

## 采用率诊断（修正版）+ 已落地修复（推翻上条方向）
- 上条方向有误：用户指出"调用率低跟少没暴露工具无关，explore 已是唯一检索工具但被 grep 替代"。重做实证：
  - explore docstring(`mcp_server.py:355-371`)只说"primary code-intelligence tool"，**从不点名 grep/Read、无排他说明**——文献(theaiuniversity/CSDN)公认的工具调错首要原因。
  - AGENTS.md(`dataAgent/AGENTS.md` 真实存在)写"Always try FIRST"太软、无负向决策规则；连 CodeGraph 自己都有此采纳问题(LobeHub 文档)。
  - 实测 explore 输出 6.4K–13.2K 字符(runScript 6455 / ScriptExecutionService 13205 等)，廉价查找被 grep 抢。
  - 预算提示 L462 "fall back to Read" 反向背书(有证据，成立)。
  - 输入契约错配：explore 只收 symbol，agent 常只有关键词→`not found` 回退 grep。
  - 冷启动"训练 agent 远离"降级为**假设**(报错真实，行为推论无证据)。
- 联网检索解法：工具描述写"何时用/不用/替代 grep"；AGENTS.md 需强指令块；**Hooks(SessionStart/UserPromptSubmit)每次提示重申**最高杠杆(mcp-codebase-index 实证)；MCP initialize 握手推送(codesearch)；决策表+负向约束。
- **已落地修复**(用户确认"先同步诊断，再改工具描述、agents.md")：
  1. 重写诊断第九章为证据版(删跑偏 RC-1、RC-5 降假设、加联网解法与按证据强度根因表)。
  2. `mcp_server.py` explore docstring 加 "Prefer this over Grep/Read for any code lookup; only use Grep/Read for known file paths…" 排他说明。
  3. explore `not found` 时**自动 FTS 回退给候选列表**(修输入契约错配 RC-3)：实测 `codewiki_explore("ExecutionService")` 返回 8 个候选。
  4. 预算提示 L462 改为 "Keep using codewiki_explore for further graph queries — do not fall back to Read"。
  5. `_CODEWIKI_AGENTS_SECTION` 改写为强指令块+决策表+负向约束；并同步更新已部署的 `dataAgent/AGENTS.md`。
- 冒烟测试通过：import 正常、有效符号 explore 正常、not-found 优雅返回候选/无匹配消息。未跑全量 pytest(改动为文本+一处 guarded 回退)。
- 待评估：B 加 MCP 钩子(最高杠杆)、E 修 8K 预算幌子(代码只限节点数不截断字符，ScriptExecutionService 13.2K 超 8K 标注)、F 埋点度量调用率。

## 架构改造：MCP=1 工具（explore only），init/sync 改 CLI-only（2026-07-14 下午）

**决策**（用户直接指令）：
- MCP 只暴露 `codewiki_explore`（纯只读查询）。`init`/`sync` 仅 CLI，不进 MCP handler。
- auto_sync（进程内 10min 轮询）完全移除。不保留。不加 `--root`。

**改动**：
- `mcp_server.py`：删 `codewiki_init`/`codewiki_sync` 两个 `@mcp.tool()` + `_start_auto_sync_if_enabled` 函数及其调用；`codewiki_explore` 顶部补 `_ensure_store()` 自动打开已有库（替代之前的硬 "No project indexed. Use codewiki_init first."）；AGENTS 段 + `_inject_agents_md` 迁出；build imports 移除。
- 新建 `src/codewiki/bootstrap.py`：`_CODEWIKI_AGENTS_SECTION` + `inject_agents_md(project_root)`（供 CLI init 调用）。模板更新：明确构建由 CLI 完成、agent 只有 explore、explore 说"未索引"则告知用户跑 `codewiki init`。
- `cli.py`：`init` 末尾调 `inject_agents_md(root)`；移除 `--no-auto-sync` 选项与 `config.json` `auto_sync` 字段；serve docstring 去 auto_sync。
- `dataAgent/AGENTS.md`：Tools 段更新（去 init/sync，加 Building/refreshing NOT an MCP tool 段）。
- 测试：`test_p3.py` M5 config 测试（auto_sync→branch）；待 pytest 跑完确认。
- 文档同步：`项目整体方向.md`（触发层/查询层/增量方案/同步机制/P2-P3行）、`技术架构与开发安排.md`（架构图/diagram/P2-P3/config.json）、`收敛状态评估.md`（M5）、`P3-Git增量同步.md`（触发/约束/验收/Done）、`P2-MCP查询服务.md`（工具面/约束/验收）、`AGENTS_模板.md`、`问题诊断与根因分析.md` 第九章补架构决策节。

