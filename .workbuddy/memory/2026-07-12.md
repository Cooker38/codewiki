# 2026-07-12 工作日志

## 技术栈选型完成（Java → Python）
- **回退 Java 方案**（生态重、MCP 里程碑版风险、tree-sitter 个人维护风险）
- **定为 Python 3.10+**：官方 MCP SDK (FastMCP) + 官方 tree-sitter (py-tree-sitter) 双保险；运行时依赖仅 10 个，精简可控
- **LangGraph** 用于 Wiki 管线（StateGraph 多阶段 + Send 并行 fan-out + SqliteSaver checkpoint + subagent），非对话式 Agent
- 完整技术栈见 `resource/技术架构与开发安排.md` 第六节

## 文档产出
- `项目整体方向.md`：第六节技术选型重写（Python 全栈 6 子节）+ 新增第七节开发阶段
- **新增** `技术架构与开发安排.md`：系统架构图、数据流（3 条）、目录结构（项目侧 + 产物侧）、开发阶段（5 阶段依赖关系）、技术约定、环境搭建
- MEMORY.md 同步更新（技术栈 + 开发阶段 + 参考源）

## 已确认全部决策汇总
- Wiki = 可见文件（AGENTS.md → quickstart.md → 内链），图谱 = MCP 工具，两套不混
- CLI = MCP（统一工具面，`codewiki init/sync` 用户和 Agent 都能调）
- auto_sync 默认开启（MCP 进程生命周期 = 守护周期，.codewiki/config.json 可关闭）
- AGENTS.md/CLAUDE.md（生态标准）不用 CODEWIKI.md
- 不用 prompt-hook、不用 wiki MCP 工具
- 开发语言 Python，分 5 阶段推进，P1 被解析目标语言 Java（tree-sitter-java）

## P1 M1 完成
- 项目骨架创建：pyproject.toml + src/codewiki/ 目录结构（extraction/languages, db, resolution/frameworks, graph, search）
- M1 代码交付：
  - `types.py`：22 NodeKind + 12 EdgeKind + Node/Edge/FileRecord/UnresolvedReference/ExtractionResult/ExtractionError/SearchResult 全部 dataclass
  - `db/schema.sql`：6 表 + FTS5 + 3 触发器 + 全部索引（逐行对齐 codegraph）
  - `db/store.py`：GraphStore（insert_node/edge/file/unresolved_ref + FTS5 搜索 + name_segment_vocab + 级联删除 + metadata）
  - `extraction/tree_sitter_helpers.py`：generate_node_id + get_node_text + get_child_by_field + get_preceding_docstring
  - `search/identifier_segments.py`：split_identifier_segments + normalize_prose_word + extract_prose_candidates
- 测试：24 tests 全部通过（0.15s），覆盖 node ID、segment 拆分、schema 完整性、store CRUD、FTS5 搜索、edge 去重、unresolved_ref 生命周期、级联删除、幂等性

## P1 M2 完成
- `extraction/tree_sitter.py`：TreeSitterExtractor 核心（extract/createNode/visitNode/visitFunctionBody/extractCall/extractInstantiation + class/method/field/import/interface/enum/parameter/variable 提取器）
- `extraction/languages/java.py`：JavaExtractor 配置（Lombok 合成 + 返回类型归一化 + visibility/static/const 检测 + import/package 提取）
- 测试夹具：5 个 Java 文件（DiscountService/DiscountCalculator/OrderStatus/Order with Lombok/Discount）
- 32 tests 全部通过，覆盖：节点提取（file/class/method/field/parameter/import/namespace/enum/enum_member/interface）、qualified_name/signature/return_type/docstring、contains 边层级、unresolved refs（calls/instantiates/imports）、Lombok 合成（@Data→getters/setters/equals/hashCode/toString，@Getter/@Setter 字段级，不覆盖已声明方法）
- M1+M2 合计 56 tests 全通过（0.39s）

## P1 M3 完成
- `extraction/orchestrator.py`：ExtractionOrchestrator（scan_directory + index_all + store_extraction_result + content_hash + git ls-files 快路径）
- `extraction/parse_pool.py`：ParsePool（ProcessPoolExecutor 并行解析，pool_size = clamp(cpu-1, 1, 8)）
- `resolution/frameworks/java.py`：Spring Boot 检测（pom.xml/build.gradle + @Service/@RestController 等注解扫描）
- 测试夹具新增：OrderService.java（含 @Service 注解）
- 24 tests 全部通过，覆盖：文件扫描（git ls-files + 文件系统 walk fallback）、框架检测（Spring）、全量建库（多文件→SQLite）、FileRecord（content_hash）、contains 边、unresolved_refs（pending 跨文件引用）、name_segment_vocab、FTS5 搜索、Lombok 节点、metadata 存储、幂等性（两次建库结果一致）
- 修复：store.py 改用 isolation_level=None 手动事务管理，解决"cannot start a transaction within a transaction"
- M1+M2+M3 合计 80 tests 全通过（2.00s）

## P1 M4 完成
- `resolution/import_resolver.py`：JVM import 解析（qualifiedName 匹配，`import com.example.Foo` → `com.example::Foo`）
- `resolution/name_matcher.py`：策略链（dotted call chain `Foo.getInstance().bar` → method call `receiver.method` → exact name match，same-file 优先）
- `resolution/resolver.py`：ReferenceResolver（resolve_and_persist：遍历 pending → 策略链解析 → 创建 edges → 删 resolved / mark failed 留 name_tail）
- `resolution/callback_synthesizer.py`：合成层（type_of: field→类型类节点 / returns: method→返回类型类节点 / overrides: 子类方法→父类同 名方法，均 provenance='heuristic'，幂等）
- 14 tests 全部通过，覆盖：pending=0、failed 有 name_tail、跨文件 calls 解析（calculateDiscount→getRate）、instantiates 边、provenance 标注、内部调用解析、解析统计、type_of/returns/overrides 合成、合成幂等性、无重复边、边完整性
- M1+M2+M3+M4 合计 94 tests 全通过（2.97s）

## P1 M5 完成 — P1 全部完成
- `graph/traversal.py`：GraphTraversal（callers/callees/impact/node/search）
  - callers：BFS 反向沿 calls/references/imports/instantiates，先 visited 后深度（#1086），计入 instantiates（#774）
  - callees：BFS 正向同款边种类
  - impact：BFS 反向排除 contains（#536 防爆炸），容器节点展开到子方法（同深度递归）
  - node：按 ID 或 name 查询（大小写不敏感 fallback）
  - search：FTS5 BM25 搜索（空查询返回空列表）
- 23 tests 全部通过，覆盖：callers 返回调用方+实例化方、callees 返回被调用方+实例化目标、无重复（#1086）、深度限制、impact 返回依赖方、impact 排除 contains、impact 容器展开到子方法、node 按 name/ID/大小写不敏感查询、search FTS5 搜索+score+limit+空查询
- 修复：store.py search_nodes_fts 空查询返回空列表（FTS5 不接受空 MATCH）
## P2 MCP 查询服务 完成
- `resource/specs/P2-MCP查询服务.md` 落盘（收敛目标 + 范围7行做/不做 + 约束5条 + 验收10条）
- `src/codewiki/mcp_server.py`：FastMCP server（8 tools：init/sync/node/callers/callees/impact/search/explore）+ explore 预算算法（对齐 codegraph getExploreOutputBudget，按索引文件数分级裁剪）
- 16 tests 全部通过（node/callers含instantiates/callees/impact/search/explore预算/init建图/未初始化不崩溃）
- P1+P2 合计 133 tests 全通过（5.17s）

